export type LeadPayload = {
  name: string;
  phone?: string;
  device: string;
  message: string;
  status?: string;
  urgency?: string;
  submittedAt?: string;
  pagePath?: string;
  source?: string;
};
